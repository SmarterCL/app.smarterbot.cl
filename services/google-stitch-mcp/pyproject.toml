[project]
name = "google-stitch-mcp"
version = "0.1.0"
description = "Google Stitch MCP Service"
requires-python = ">=3.14"
dependencies = [
    "mcp>=0.1.0",
    "httpx>=0.25.0",
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
