[project]
name = "smarteros-root"
version = "0.1.0"
description = "SmarterOS Unified Python Backend & MCP Services"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
    "pydantic>=2.9.0",
    "httpx>=0.27.0",
]

[tool.uv]
workspace = { members = ["services/*"] }
